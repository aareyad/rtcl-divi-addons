;(function ($) {
    $(document).ready(function () {
        /*window.addEventListener("et_builder_api_ready", function () {
            $(".rtcl-listings-slider-container").each(function () {
                $(this).rtcl_slider();
            });
        });
        window.addEventListener('et_fb_page_load', function () {
            console.log('Divi Frontend Editor fully loaded');
        });*/
        // et_load_event_fired
        // et_fb_page_load
        /*if ($(".rtcl-listings-slider-container").length) {
            console.log('Divi Frontend Editor fully loaded');
        }
        window.addEventListener('et_fb_page_load', function () {
            console.log('Divi Frontend Editor fully loaded');
        });
        window.addEventListener('DOMContentLoaded', function () {
            console.log('Divi loading editor');
            $(".rtcl-listings-slider-container").each(function () {
                $(this).rtcl_slider();
            });
        });*/
        /*var waitForElements = setInterval(function () {
            let sliders = $(".rtcl-listings-slider-container");
            console.log(sliders);
            console.log('Divi loaded');
            if (sliders.length) {
                console.log(sliders);
                console.log('Divi elements loaded');
                clearInterval(waitForElements);
                /!*sliders.each(function () {
                    $(this).rtcl_slider();
                });*!/
                sliders.forEach(function (slider) {
                    slider.rtcl_slider();
                })
            }
        }, 500);*/
    });
}(jQuery));